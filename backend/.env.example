# Database
DATABASE_URL="postgresql://user:password@localhost:5432/buena_properties"

# AI Extraction — all optional, regex parser works without any key
# Auto chain: regex → Gemini → Ollama

# Gemini — primary AI fallback (recommended)
# Get a key at https://aistudio.google.com
GEMINI_API_KEY=""

# Ollama — free local AI fallback, no key needed
# Requires Ollama running locally: https://ollama.com → `ollama run llama3.2`
OLLAMA_BASE_URL="http://localhost:11434"
# OLLAMA_MODEL="llama3.2"

# OpenAI — wired up but untested
OPENAI_API_KEY=""
